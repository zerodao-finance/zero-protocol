{
  "name": "@zerodao/zerodao",
  "version": "0.1.0",
  "description": "zeroDAO Protocol",
  "repository": "https://github.com/zerodao-finance/zero-protocol",
  "scripts": {
    "contracts": "yarn workspace @zerodao/contracts",
    "protocol": "yarn workspace @zerodao/protocol"
  },
  "author": "zeroDAO",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "prettier": {
    "overrides": [
      {
        "files": "*.sol",
        "options": {
          "tabWidth": 2,
          "printWidth": 80,
          "bracketSpacing": true
        }
      }
    ]
  }
}
