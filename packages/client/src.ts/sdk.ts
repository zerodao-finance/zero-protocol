import { EventEmitter } from "events";

class SDKTransfer extends EventEmitter {

    log: Logger;
    conn: ConnectionType;
    constructor( chainId: number, zeroUser, value, token, signer, to, isFast, _data ) {
        super()
    }

    
}


